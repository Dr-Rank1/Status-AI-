[package]
name = "status_meta_compiler"
version = "0.1.0"
edition = "2021"
description = "Phase 43 — Autonomous meta-compiler daemon (zero-trust sandboxed rewrites)"

[lib]
name = "status_meta_compiler"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
