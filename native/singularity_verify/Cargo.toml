[package]
name = "status_singularity_verify"
version = "0.1.0"
edition = "2021"
description = "Phase 45 — Transcendental singularity / human-alignment axiom verifier"

[lib]
name = "status_singularity_verify"
crate-type = ["cdylib", "rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
